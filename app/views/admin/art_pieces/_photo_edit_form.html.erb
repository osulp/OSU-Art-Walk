<div id="picture">
  <div class="fields">
    <%= f.input :photo, :as => :file, :required => false unless f.object.photo_url%>
    <%= image_tag f.object.photo_url(:thumb) %>
    <%= f.hidden_field :_destroy %>
    <%= link_to_remove_fields "remove", f %>
  </div>
</div>
